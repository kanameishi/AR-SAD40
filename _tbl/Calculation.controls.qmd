```{r}
#| include: false
if (!exists("CAP", inherits = FALSE)) {
  CAP <- paste(readLines("_captions/tbl.Calculation.controls.md"), collapse = "\n")
}
source("scripts/tbl/Calculation.controls.R")
TBL <- buildCalculationControlsTable(
  BiaxialControl$paths$controls
)
```

```{r}
#| label: tbl-calculation-controls
#| tbl-cap: !expr CAP
#| echo: false
TBL
```

```{r}
#| include: false
rm(CAP, TBL, buildCalculationControlsTable)
```
