```{r}
#| include: false
if (!exists("CAP", inherits = FALSE)) {
  CAP <- paste(
    readLines("_captions/tbl.Calculation.aashto.checks.ES.md", warn = FALSE),
    collapse = "\n"
  )
}
source("scripts/tbl/Calculation.aashto.checks.R")
TBL <- buildCalculationAashtoChecksTable(Calculation$paths$aashtoChecks)
```

```{r}
#| label: tbl-calculation-aashto-checks
#| tbl-cap: !expr CAP
#| echo: false
TBL
```

```{r}
#| include: false
rm(CAP, TBL, buildCalculationAashtoChecksTable)
```
