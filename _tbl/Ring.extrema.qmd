```{r}
#| include: false
if (!exists("CAP", inherits = FALSE)) {
  CAP <- paste(
    readLines("_captions/tbl.Ring.extrema.md", warn = FALSE),
    collapse = "\n"
  )
}
source("scripts/tbl/Ring.extrema.R")
if (!exists("CaseLabels", inherits = FALSE)) {
  CaseLabels <- c(
    "membrane-control" = "Membrane",
    "SAD40-3x1-3mm-preliminary" = "76x25x3 mm section"
  )
}
TBL <- buildRingExtremaTable(
  path = "TITO/kb/benchmarks/corrugated-k0-extrema.csv",
  caseLabels = CaseLabels
)
```

```{r}
#| label: tbl-ring-extrema
#| tbl-cap: !expr CAP
#| echo: false
TBL
```

```{r}
#| include: false
rm(CAP, CaseLabels, TBL, buildRingExtremaTable)
```
