```{r}
#| include: false
if (!exists("CAP", inherits = FALSE)) {
  CAP <- paste(
    readLines("_captions/tbl.liner.existing.interaction.ES.md", warn = FALSE),
    collapse = "\n"
  )
}
source("scripts/tbl/Calculation.interaction.R")
TBL <- buildCalculationSchwartzEinsteinTable(
  Calculation$paths$schwartzEinstein
)
```

```{r}
#| label: tbl-liner-existing-interaction
#| tbl-cap: !expr CAP
#| echo: false
TBL
```

```{r}
#| include: false
rm(CAP, TBL, buildCalculationInteractionTable,
  buildCalculationSchwartzEinsteinTable)
```
