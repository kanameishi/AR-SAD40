```{r}
#| include: false
if (!exists("CAP", inherits = FALSE)) {
  CAP <- paste(
    readLines("_captions/tbl.liner.rehabilitation.plain.checks.ES.md", warn = FALSE),
    collapse = "\n"
  )
}
source("scripts/tbl/Calculation.shotcrete.checks.R")
TBL <- buildCalculationShotcreteChecksTable(
  path = Calculation$paths$shotcreteChecks,
  liningID = c("shotcrete", "plainConcrete150"),
  thicknessMm = c(
    shotcrete = 1000 * Calculation$shotcrete$section$thicknessM,
    plainConcrete150 =
      1000 * Calculation$reinforcedConcrete$section$thicknessM
  )
)
```

```{r}
#| label: tbl-liner-rehabilitation-plain-checks
#| tbl-cap: !expr CAP
#| echo: false
TBL
```

```{r}
#| include: false
rm(CAP, TBL, buildCalculationShotcreteChecksTable)
```
