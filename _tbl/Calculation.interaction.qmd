```{r}
#| include: false
if (!exists("CAP", inherits = FALSE)) {
  CAP <- paste(
    readLines("_captions/tbl.Calculation.interaction.ES.md", warn = FALSE),
    collapse = "\n"
  )
}
source("scripts/tbl/Calculation.interaction.R")
TBL <- buildCalculationSchwartzEinsteinTable(
  Calculation$paths$schwartzEinstein
)
```

```{r}
#| label: tbl-calculation-interaction
#| tbl-cap: !expr CAP
#| echo: false
TBL
```

```{r}
#| include: false
rm(CAP, TBL, buildCalculationSchwartzEinsteinTable)
```
