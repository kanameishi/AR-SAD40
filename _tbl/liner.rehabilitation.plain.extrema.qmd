```{r}
#| include: false
if (!exists("CAP", inherits = FALSE)) {
  CAP <- paste(
    readLines("_captions/tbl.liner.rehabilitation.plain.extrema.ES.md", warn = FALSE),
    collapse = "\n"
  )
}
source("scripts/tbl/Calculation.extrema.R")
TBL <- buildCalculationExtremaTable(
  pathResultants = Calculation$paths$shotcreteResultants,
  pathExtrema = Calculation$paths$shotcreteExtrema,
  liningID = "shotcrete"
)
```

```{r}
#| label: tbl-liner-rehabilitation-plain-extrema
#| tbl-cap: !expr CAP
#| echo: false
TBL
```

```{r}
#| include: false
rm(CAP, TBL, buildCalculationExtremaTable)
```
